# TBS-Client (TheBlockSurvival) → MC 26.2 — staging status

**Branch:** `release/26.2` · **Started:** 2026-06-19 · **Status:** STAGED, cutover HELD.

⚠️ **Staging only.** Client + server must ship 26.2 **in lockstep** (a 26.2 client can't join the
26.1.2 server), so this is held until TBS-Server's 26.2 cutover gate clears. Not published / not
imported into the live launcher instances.

## What was done
- `pack.toml`: `minecraft 26.1.2 → 26.2`, `fabric 0.19.2 → 0.19.3`,
  `acceptable-game-versions ["1.21.11"] → ["26.2"]`.
- `packwiz update --all` → **33 mods updated to 26.2** (Sodium, Iris, JEI, Lithium, 3D Skin Layers,
  Entity Culling, …), indexes refreshed.

## Blockers with no 26.2 build yet (6) — none world-critical (client-side by design)
Re-check with `packwiz update --all`.

- **StreamCraft Live** (first-party, `side=both`) — must be the **same version as TBS-Server**; build
  the 26.2 jar once and sync both packs.
- **FerriteCore** — perf lib (also a server blocker).
- **voxy** — far-render; client QoL.
- **WTHIT** — item/block tooltip HUD; client QoL.
- **First-person Model** — cosmetic.
- **Patrix 32x** — resourcepack (texture pack), not a functional mod.

Per repo convention these belong in `README.md` "Pending mods" + `CHANGELOG.md` "Not yet included"
at release time — recorded here for the staging checkpoint.

## Cutover (LATER, lockstep with TBS-Server)
Drop/keep the QoL blockers per preference; StreamCraft version pinned to the server's. Hold until the
server gate clears. Don't bump StreamCraft here alone (synchronized release of both packs).
